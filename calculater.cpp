#include <iostream>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;

    // code here
    int p,q,r,s,t;
    
    p=x+y;
    q=x-y;
    r=x*y;
    s=x/y;
    t=x%y;

    // The below code prints the output
    cout << p << " " << q << " " << r << " " << s << " " << t << endl;
    return 0;
}
